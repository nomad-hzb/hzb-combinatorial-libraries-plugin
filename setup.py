from setuptools import setup, find_packages

if __name__ == "__main__":
    setup(
        # name='hzb_combinatorial_libraries',
        # package_dir={'': 'hzb_combinatorial_libraries'},
        # packages=find_packages(where='hzb_combinatorial_libraries'),
    )
